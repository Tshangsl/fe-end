css常用技巧

overflow:hidden，那么该元素的内容若超出了给定的宽度和高度属性，那么超出的部分将会被隐藏，不占位。

溢出隐藏
清除浮动
解决外边距塌陷

body标签高度不是浏览器界面 而是里面元素的总和高度
html标签也是内容撑开的
body->html->document（当前文档）->window(整个浏览器)
***使div充满屏幕的两种方法

**块元素的水平垂直居中
1.
设置对应块元素的水平垂直居中
margin:0 auto;
设置对应块元素的水平居中
相对定位
绝对定位 
margin 具体可看test4_4.html
外层块元素水平居中
内层块元素相对于外层块元素 设置水平 垂直居中
2.
css3中 ie8之前不兼容
设置对应行元素的水平垂直居中
**行级元素的垂直水平居中，当作外层元素文本设置 单/多行元素的垂直居中
元素的定位 不是元素内容的定位
需要知道宽度和高度的具体值 
text-aglin:center;
把其当作文本元素进行设置


如果子元素定位 以上都不好使 

***两列布局 左侧固定 右侧自适应

绝对定位 不设置宽度 宽度就是内容撑开的
float（浮动） 定位 不给宽度 宽度是内容撑开

宽高百分比永远都是相对于父元素 

***三列布局 两侧固定 中间自适应

***水平导航菜单
所有的菜单都用列表来做
ul#menu>li*3>a
li默认垂直分布 

display使块元素水平不要用了





解决子元素浮动 导致父元素没有高度 在父元素上加clearfix


css3 交互式响应