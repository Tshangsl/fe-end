全局组件必须写在Vue实例创建之前，才在该根元素下面生效

关于组件中的其他属性，可以和实例中的一样，
但是data属性必须是一个函数

全局组件和局部组件一样，data也必须是一个函数：

全局组件和局部组件的作用域问题:
只要变量是在组件内部用的 
这些变量必须是组件内部的
而在外部html结构中引用的变量
都引用的是挂载下的实例里面的变量

Vue和小程序等一样，采用es6的函数写法 this指向是不一样的

--------------------------------

vue组件注册
组件名
在注册一个组建的时候 我们始终需要给它一个名字
该组件名就是Vue.component的第一个参数

w3c规范 
自定义组件名（字母全小写且必须包含一个连字符）
帮助避免和当前以及未来的HTML元素冲突

两种组件命名方式 短横线 驼峰法
通过Vue.component注册的组件都是全局注册的

Vue.js还提供了局部注册的机制

全局注册往往是不够理想的 比如
如果你使用一个像webpack这样的构建系统
全局注册所有的组件一位置即使你已经不在使用一个组件了
它仍然会被包含在你最终的构建结果中
者造成了用户下载的Javascript的无谓的增加

在这些情况下 你可以通过一个普通的javascript对象来定义组件
var Component={}
然后在components选项中定义你想要使用的组件
