对象/两种创建属性并赋值的方法/递归遍历

函数：（不加分号）
语句块  不需要加分号
函数表达式：（加分号）
语句 需要加分号

this指向当前对象 
obj可以在任意位置使用 
this只能使用在对象的方法中

**对象都是放在堆里的

*访问对象中不存在的一个属性时  不会报错 会返回undefined

*property 属性
*scope 作用域
*argument参数
*constructor (function)构造函数

方法看作一个特殊的属性

*创建属性可以用[]方式 可以使用变量调用

对象直接量 literal {}给一个对象赋值 最后加分号 括号内中间用逗号隔开
prop 返回的永远是属性的名字
typeof 返回的是字符串 

对象的深拷贝copy

原型链的查找过程 访问对象属性
构造函数 原型 是否有父类（父类原型中找到） 最高找到Object没有返回undefined 找到即返回
Object默认是所有类的父类